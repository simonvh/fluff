language: python

python:
  - "2.7"

cache:
  apt: true
  directories:
    - $HOME/miniconda

before_install:
  # install miniconda for Python dependencies
  - wget https://repo.continuum.io/miniconda/Miniconda2-latest-Linux-x86_64.sh -O miniconda.sh
  - chmod +x miniconda.sh
  - ./miniconda.sh -b -p $HOME/miniconda -f
  - export PATH=$HOME/miniconda/bin:$PATH
  - conda update --yes conda
  - conda info -a

install:
  - conda install --yes python=$TRAVIS_PYTHON_VERSION numpy scipy matplotlib pytest 
  - conda install --yes -c bioconda python=$TRAVIS_PYTHON_VERSION pysam htseq pybedtools bedtools colorbrewer Pycluster pyBigWig
# # command to run tests, e.g. python setup.py test
script:  
  - python setup.py test
 
#after_sucess:
#  coveralls
